/**
 * Este paquete contiene las interfaces DAO (Data Access Object) para el sistema de carrito de compras.
 *
 * Las interfaces definen los métodos que deben implementar las clases de acceso a datos para realizar
 * operaciones CRUD (crear, leer, actualizar, eliminar) sobre los diferentes modelos del sistema.
 *
 * <p>Estas interfaces permiten desacoplar la lógica de negocio del detalle de almacenamiento,
 * facilitando el uso de múltiples fuentes de datos (memoria, archivos, bases de datos, etc.).
 *
 * <p>Ejemplos de interfaces:
 * <ul>
 *   <li>{@code UsuarioDAO}: define métodos para gestionar usuarios.</li>
 *   <li>{@code CarritoDAO}: operaciones relacionadas con carritos de compras.</li>
 *   <li>{@code ProductoDAO}: CRUD de productos.</li>
 * </ul>
 *
 * @author Keyra
 */

package ec.edu.ups.dao;